const zlib=require('zlib');
const FILE_ID='15TcqNsc3oLqzJxjZ8whV-cgYX00HM2k9';
const SOURCE_NAME='Palu-IDP KI.xlsx';
const SOURCE_MODIFIED_AT='2026-07-24T14:45:10.558Z';
let cache={book:null,t:0};
function norm(v){return String(v||'').toLowerCase().replace(/\(\s*20\d{2}\s*\)/g,'').replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ')}
function isZip(b){return b&&b.length>4&&b[0]===0x50&&b[1]===0x4b}
async function getBuffer(url,timeout=8500){const c=new AbortController(),timer=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{redirect:'follow',headers:{'User-Agent':'ETOS-ID-Palu-IDP/36.1'},signal:c.signal});const b=Buffer.from(await r.arrayBuffer());if(!r.ok||!isZip(b))throw new Error(`Drive fallback HTTP ${r.status}`);return b}finally{clearTimeout(timer)}}
async function download(){let last=null;for(const url of [`https://drive.usercontent.google.com/download?id=${FILE_ID}&export=download&confirm=t`,`https://drive.google.com/uc?export=download&id=${FILE_ID}&confirm=t`]){try{return await getBuffer(url)}catch(e){last=e}}throw last||new Error('Workbook IDP fallback tidak dapat diunduh.')}
function zipMap(buf){let e=-1;for(let i=buf.length-22;i>=Math.max(0,buf.length-66000);i--)if(buf.readUInt32LE(i)===0x06054b50){e=i;break}if(e<0)throw new Error('XLSX ZIP invalid.');let off=buf.readUInt32LE(e+16),count=buf.readUInt16LE(e+10),m=new Map();for(let n=0;n<count;n++){if(buf.readUInt32LE(off)!==0x02014b50)break;const method=buf.readUInt16LE(off+10),cs=buf.readUInt32LE(off+20),nl=buf.readUInt16LE(off+28),xl=buf.readUInt16LE(off+30),cl=buf.readUInt16LE(off+32),lo=buf.readUInt32LE(off+42),name=buf.slice(off+46,off+46+nl).toString();m.set(name,{method,cs,lo});off+=46+nl+xl+cl}return m}
function entry(buf,map,name){const x=map.get(name);if(!x)return null;const lo=x.lo,nl=buf.readUInt16LE(lo+26),el=buf.readUInt16LE(lo+28),start=lo+30+nl+el,d=buf.slice(start,start+x.cs);return x.method===8?zlib.inflateRawSync(d):d}
function dec(s){return String(s||'').replace(/&quot;/g,'"').replace(/&apos;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&amp;/g,'&')}
function attr(s,k){const m=String(s).match(new RegExp('(?:^|\\s)'+k+'="([^"]*)"'));return m?dec(m[1]):''}
function col(ref){let n=0;for(const c of (String(ref).match(/^[A-Z]+/)||['A'])[0])n=n*26+c.charCodeAt(0)-64;return n}
function parseBook(buf){const zm=zipMap(buf),wbe=entry(buf,zm,'xl/workbook.xml'),re=entry(buf,zm,'xl/_rels/workbook.xml.rels');if(!wbe||!re)throw new Error('Struktur workbook IDP tidak valid.');const wb=wbe.toString(),rels=re.toString(),rel={};for(const m of rels.matchAll(/<Relationship\b([^>]*)\/?\s*>/g))rel[attr(m[1],'Id')]=attr(m[1],'Target');let shared=[];const ss=entry(buf,zm,'xl/sharedStrings.xml');if(ss){for(const m of ss.toString().matchAll(/<si\b[^>]*>([\s\S]*?)<\/si>/g)){let t='';for(const q of m[1].matchAll(/<t\b[^>]*>([\s\S]*?)<\/t>/g))t+=dec(q[1]);shared.push(t)}}const sheets=[];for(const m of wb.matchAll(/<sheet\b([^>]*)\/?\s*>/g)){const title=attr(m[1],'name'),rid=attr(m[1],'r:id'),target=rel[rid];if(!target)continue;const raw=entry(buf,zm,'xl/'+target.replace(/^\//,'').replace(/^xl\//,''));if(!raw)continue;const xml=raw.toString(),vals=new Map();let maxR=0,maxC=0;for(const c of xml.matchAll(/<c\b([^>]*)>([\s\S]*?)<\/c>/g)){const ref=attr(c[1],'r'),r=Number((ref.match(/\d+$/)||['0'])[0]),cc=col(ref),typ=attr(c[1],'t');if(!r||r>400||cc>40)continue;maxR=Math.max(maxR,r);maxC=Math.max(maxC,cc);let v='',vm=c[2].match(/<v[^>]*>([\s\S]*?)<\/v>/);if(typ==='s'&&vm)v=shared[Number(vm[1])]||'';else if(typ==='inlineStr'){for(const q of c[2].matchAll(/<t\b[^>]*>([\s\S]*?)<\/t>/g))v+=dec(q[1])}else if(vm)v=dec(vm[1]);if(String(v).trim())vals.set(r+':'+cc,String(v))}const values=[];let filledRows=0,filledCells=0,lastRow=0,lastColumn=0;for(let r=1;r<=maxR;r++){const row=[];let used=false;for(let cc=1;cc<=maxC;cc++){const v=vals.get(r+':'+cc)||'';row.push(v);if(v){used=true;filledCells++;lastColumn=Math.max(lastColumn,cc)}}while(row.length&&!row[row.length-1])row.pop();values.push(row);if(used){filledRows++;lastRow=r}}while(values.length&&!values[values.length-1].length)values.pop();sheets.push({title,values,summary:{filledRows,filledCells,lastRow,lastColumn,truncated:maxR>=400||maxC>=40}})}return sheets}
async function load(force=false){if(!force&&cache.book&&Date.now()-cache.t<300000)return cache.book;const book=parseBook(await download());cache={book,t:Date.now()};return book}
function usable(book){return(book||[]).filter(s=>!['definisi umum','nama etoser','template','petunjuk'].includes(norm(s.title)))}
async function overview(awardees,force=false){const book=usable(await load(force)),idx=new Map();for(const s of book){const k=norm(s.title);if(!idx.has(k))idx.set(k,[]);idx.get(k).push(s)}const items=(awardees||[]).map(a=>{const ms=idx.get(norm(a.name))||[],s=ms.length===1?ms[0]:null;return{id:a.legacy_id,nama:a.name,angkatan:a.angkatan,status:a.status,connected:!!s,sheetName:s?.title||'',...(s?.summary||{filledRows:0,filledCells:0,lastRow:0,lastColumn:0,truncated:false})}});return{sourceName:SOURCE_NAME,sourceId:FILE_ID,sourceMode:'drive-verified-fallback',sourceModifiedAt:SOURCE_MODIFIED_AT,sourceUrlHint:'Verified read-only fallback workbook',stale:true,totalActive:items.length,connected:items.filter(x=>x.connected).length,missing:items.filter(x=>!x.connected).length,items}}
async function detail(name,cohort='',force=false){const book=usable(await load(force)),candidates=[norm(name),norm(`${name} (${cohort})`)];const matches=book.filter(s=>candidates.includes(norm(s.title)));if(matches.length!==1)throw new Error(matches.length?'Nama tab IDP fallback ambigu.':'Tab IDP fallback tidak ditemukan.');const s=matches[0];return{nama:name,sheetName:s.title,sourceName:SOURCE_NAME,sourceId:FILE_ID,sourceMode:'drive-verified-fallback',sourceModifiedAt:SOURCE_MODIFIED_AT,stale:true,values:s.values,summary:s.summary}}
async function health(force=false){const book=usable(await load(force));return{edge:'degraded',source:'google-drive-verified-fallback',mode:'drive-verified-fallback',sourceId:FILE_ID,title:SOURCE_NAME,sourceModifiedAt:SOURCE_MODIFIED_AT,sheetCount:book.length,stale:true}}
module.exports={FILE_ID,SOURCE_NAME,SOURCE_MODIFIED_AT,overview,detail,health};
